{
  "name": "@rag-next/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/main.ts --format esm",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --pool vmThreads --poolOptions.vmThreads.singleThread true",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.17",
    "@nestjs/core": "^10.4.17",
    "@nestjs/platform-express": "^10.4.17",
    "@langchain/core": "^1.1.49",
    "@rag-next/shared-types": "workspace:*",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "fflate": "^0.8.3",
    "multer": "^2.2.0",
    "pg": "^8.21.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
